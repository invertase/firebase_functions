name: basic_example
description: Basic example of Firebase Functions for Dart
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  firebase_functions:
    path: ../../
  shelf: ^1.4.2

dev_dependencies:
  build_runner: ^2.10.5
  lints: ^6.0.0
